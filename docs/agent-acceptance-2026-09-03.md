# Agent 本地链路验收 · 2026-09-03

## 本次修复

- 沿用 LLM 填槽、服务端规则、会话草稿与工作流检查点；未引入 RAG、向量库或新的编排服务。
- 文字提示词作为独立 artifact 保存、恢复、展开和复制；执行参数 prompt 为空不再等于没有交付正文。
- 拒绝未填写的模板、混合多个候选的正文；旧会话中的未定稿内容不能被隐式选中执行。
- 修正“不要生成媒体”导致提示词保存分支被跳过，以及“不要模板”被当作索要模板的否定语义漏洞。
- 验收用户明确禁止的效率百分比/倍数、明确限定的旁白总汉字数。未通过时最多调用一次当前文字模型修复；仍不合格则阻止执行。
- 扩充五类可编辑策略：总体指导、意图填槽、研究事实、创作声音、记忆恢复。发布次数不是智能等级；新增内容修复次数 0–1 的配置与费用说明。
- 合成前读取真实配音时长，共享各分镜的时间余量，保持总时长和音高；仍不足时有上限调速，无法容纳则保留素材并报错，不强行截断。
- 续传改为页面内确认卡，支持取消，确认前不入队；保留现有素材，只恢复未完成阶段。

## 真实失败项目恢复

原会话：`conv_2256458f-75d`；项目：`wfp_68fea126-259`。

此前两段配音实际 4.64 秒、7.68 秒，成片被平均切为 6+6 秒，导致第二段超过语速上限；两段素材和配音本身均已成功。

2026-09-03 17:18:34，本地在线 Worker 经用户端确认续传后完成原项目：

| 验收项 | 结果 |
| --- | --- |
| 原项目状态 | succeeded |
| 新执行节点 | compose；未重新生成关键帧、视频、配音 |
| 媒体任务表总数 | 112 → 112 |
| 项目累计费用 | 2.190281 → 2.190281 |
| 最终时长 | 浏览器读取 12.000 秒，可播放 |
| 两段画面时长 | 4.533333 秒、7.466667 秒 |
| 配音调速 | 1.041916、1.039711 倍 |
| 成品展示 | 独立“您的视频生成成功啦！”消息，只含成品视频 |
| 过程素材 | 图片 2、视频 2、配音 2，默认折叠，可展开 |
| 重开历史 | 成片与素材均保留，无重复成功消息 |

成品对象：`works/video/wfp_68fea126-259/final_1788427113823607100.mp4`。

另有独立真实素材回放测试，写入 `.artifacts/agent-compose-replay-20260903`，不修改数据库、不调用生成模型。

## 自动化与人工链路检查

- API：`go test ./...`，含独立 SQL schema 的会话/确认/模型切换/策略回滚回归。
- Worker：`go test ./...`，配置本地 FFmpeg；含真实尾音保留、时间余量分配和多段合成测试。
- Web/Admin：分别 `npx tsc --noEmit`。
- Web：`node --experimental-strip-types --test src/components/workbench/creativeAgentWorkflow.test.mjs`，7 项通过。
- `git diff --check` 无空白错误。
- 实际当前聊天模型 GLM 4.6V 的文字验收保留在标有“【链路验收】”的会话 `conv_b0917204-9c0`。首次真实输出暴露的否定语义及效率数字漏检已补规则与回归；复测已保存独立完整提示词，未生成媒体。
- 第三轮真实文字复测触发一次自动修复（`content_repaired=true`），最终草稿 v3 保存 494 字符完整提示词；逐条统计旁白实际 23 个汉字，符合用户的合计 30 字上限。模型自报字数不作为验收依据。媒体任务仍为 112，工作流仍为 23。
- 最终 `/health`：API `ok`，Worker `ok`。本地 API 和 Worker 均已加载修复代码。

## 边界与未覆盖项

- 这次恢复保留历史脚本，不擅自替换已有画面、台词；原脚本的效率宣传不能当作核验过的事实。
- 规则检查的是可判定的约束，不能保证任意 LLM 输出都事实正确或艺术质量合格；管理员指导也不是模型训练。
- 未重跑付费图片/视频/TTS 全生成链路，验证的是既有真实素材断点恢复和实际文字交付。
- 当前单成品硬上限仍为 600 秒，不声称已支持 1–2 小时连续成片。
- 极端超长配音或不可用上游渠道仍可能失败；失败应保留检查点、明确原因，不能假报成功。
